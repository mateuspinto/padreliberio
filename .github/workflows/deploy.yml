name: Deploy web to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.47.4"
          channel: stable

      - run: flutter pub get

      # CanvasKit only (no --wasm) — matches the Makefile's `web` target.
      # Skwasm's multithreading needs Cross-Origin-Opener/Embedder-Policy
      # headers, which break the embedded camera <iframe> (CameraSection);
      # not worth the tradeoff for an app that isn't graphics-heavy.
      - run: flutter build web --release --base-href /padreliberio/

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: build/web

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
